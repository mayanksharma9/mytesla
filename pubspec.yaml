name: myapp
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod: ^2.5.1
  go_router: ^14.2.0
  # Firebase core/auth/functions
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.1
  cloud_functions: ^5.1.3
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.1
  # HTTP
  dio: ^5.5.0
  # UI / Animations
  flutter_animate: ^4.5.0
  lottie: ^3.1.2
  # Utils
  intl: ^0.19.0
  collection: ^1.18.0
  google_fonts: ^6.3.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
